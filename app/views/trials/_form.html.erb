<%= form_for(@trial, :html => { :multipart => true }) do |f| %>
  <% if @trial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trial.errors.count, "error") %> prohibited this trial from being saved:</h2>

      <ul>
      <% @trial.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.cktext_area :description, :toolbar=>'Full', :width=>'800px', :height=>'200px' %>
  </div>
<div class="field">
  <%= f.file_field :attachment %>
  </div>
  <div class="field">
    <%= f.label :references %><br />
    <%= f.cktext_area :references, :toolbar=>'Full', :width=>'800px', :height=>'200px' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
